```javascript
let age = 23;
age + 2; //after 2 years
```

## My Solution

1. In the first line, first our variable `age` assigned with the value of 23, now in next line we are adding 2 in age but the addition doesn't storing in any variable this will cause the variable `age` to be unchanged.